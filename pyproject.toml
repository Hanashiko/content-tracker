[project]
name = "content-tracker"
version = "0.1.0"
description = "Content tracker"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "mypy>=1.16.0",
]
